<!DOCTYPE html>
<html lang="es">

<%- include('./partials/head') %>
<body>
  <%- include('./partials/header') %>
    
    <main>
            <div class="titulo">
                <h1>All products </h1>
            </div>

<% for(let i = 0; i < products.length; i++){ %>
  <li>
    <div class="card col-11 position-relative">
      <img class="card-img-top" src= 'img/<%= products[i].image%>' alt=<%=products[i].name%>>
      <div class="card-body position-absolute titulo-juegos d-flex align-content-center">
        <h4 class="card-title flex-grow-1"><%=products[i].name%></h4>
        <p class="card-text">
          <%= products[i].price %>
        </p>
        <a href="/products/productDetail/<%= products[i].id %>" class="stretched-link"></a>
      </div>
    </div>
    </li>
    <% } %>
  </main>
    <%- include('./partials/footer') %>
</body>

</html>